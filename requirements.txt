nicegui
python-docx
python-pptx
openpyxl
pypdf
pdfplumber
# Fully-local OCR for scanned/image PDF pages (PDFium + Tesseract via liteparse).
# The Windows build bundles the English model so OCR runs fully offline; extra
# languages are downloaded from Settings. Without liteparse, image pages are
# flagged instead of read.
liteparse
cryptography
# Read Outlook .msg files (an OLE compound format); .eml and .html use the stdlib.
# This is the optional [email] pip extra — listed here so the Windows installer /
# portable bundle ship .msg support out of the box (a lean `pipx install` without
# the extra still reads .eml/.html and just flags .msg).
extract-msg
